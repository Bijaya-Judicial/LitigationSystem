<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Case Details</title>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

<style>
    body {
        background:#eef2f7;
    }

    .header {
        background:#1b263b;
        color:white;
        padding:12px 20px;
        display:flex;
        justify-content:space-between;
        align-items:center;
    }

    .card-box {
        width:50%;
        margin:40px auto;
        background:white;
        padding:25px;
        border-radius:10px;
        box-shadow:0px 0px 10px rgba(0,0,0,0.1);
    }

    .label {
        font-weight:bold;
        color:#1b263b;
    }

    .value {
        margin-bottom:10px;
    }

    .red { color:red; font-weight:bold; }
    .orange { color:orange; font-weight:bold; }
    .green { color:green; font-weight:bold; }
</style>
</head>

<body>

<!-- HEADER -->
<div class="header">
    <h5>Collectorate Litigation System - Puri</h5>
    <div>
        {{ session.get('user') }} |
        <a href="/logout" class="text-white">Logout</a>
    </div>
</div>

<!-- CARD -->
<div class="card-box">

    <h4 class="text-center mb-4">Case Details</h4>

    <p class="value"><span class="label">Case No:</span> {{case.case_no}}</p>
    <p class="value"><span class="label">Year:</span> {{case.case_year}}</p>
    <p class="value"><span class="label">Petitioner:</span> {{case.petitioner}}</p>
    <p class="value"><span class="label">Court:</span> {{case.court}}</p>
    <p class="value"><span class="label">Case Type:</span> {{case.case_type}}</p>
    <p class="value"><span class="label">Office:</span> {{case.office}}</p>
    <p class="value"><span class="label">Next Hearing:</span> {{case.next_hearing}}</p>

    <!-- DAYS LEFT -->
    {% if case.next_hearing %}
        {% set days = (case.next_hearing - date.today()).days %}
        <p class="value">
            <span class="label">Days Left:</span>
            {% if days <= 7 %}
                <span class="red">{{days}}</span>
            {% elif days <= 15 %}
                <span class="orange">{{days}}</span>
            {% else %}
                <span class="green">{{days}}</span>
            {% endif %}
        </p>
    {% endif %}

    <div class="mt-4">
        <a href="/dashboard" class="btn btn-secondary">⬅ Back</a>

        {% if session.get('role') == 1 %}
            <a href="/edit/{{case.id}}" class="btn btn-warning">Edit</a>
        {% endif %}
    </div>

</div>

</body>
</html>